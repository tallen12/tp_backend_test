volumes:
  tp_backend_test_local_postgres_data: {}
  tp_backend_test_local_postgres_data_backups: {}
  tp_backend_test_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: tp_backend_test_local_django
    container_name: tp_backend_test_local_django
    depends_on:
      - postgres
      - redis
      - mailpit
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  # Old version missing docker file, resolve it later if needed
  # postgres:
  #   build:
  #     context: .
  #     dockerfile: ./compose/production/postgres/Dockerfile
  #   image: tp_backend_test_production_postgres
  #   container_name: tp_backend_test_local_postgres
  #   volumes:
  #     - tp_backend_test_local_postgres_data:/var/lib/postgresql/18/docker
  #     - tp_backend_test_local_postgres_data_backups:/backups
  #   env_file:
  #     - ./.envs/.local/.postgres

  postgres:
    image: docker.io/library/postgres:18
    container_name: tp_backend_test_local_postgres
    ports:
      - "5432:5432"
    volumes:
      - tp_backend_test_local_postgres_data:/var/lib/postgresql
      - tp_backend_test_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: tp_backend_test_local_mailpit
    ports:
      - "8025:8025"

  redis:
    image: docker.io/redis:7.2
    container_name: tp_backend_test_local_redis
    volumes:
      - tp_backend_test_local_redis_data:/data

  celeryworker:
    <<: *django
    image: tp_backend_test_local_celeryworker
    container_name: tp_backend_test_local_celeryworker
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: tp_backend_test_local_celerybeat
    container_name: tp_backend_test_local_celerybeat
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: tp_backend_test_local_flower
    container_name: tp_backend_test_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
